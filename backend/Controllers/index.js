// this is where we handle all the data from the incoming requests we then pass it off to
// the service layer that has the interaction with the database.

const userRouter = require('./Users')


const routers = {
    userRouter
}


module.exports = routers